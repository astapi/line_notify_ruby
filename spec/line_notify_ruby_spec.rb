require 'spec_helper'

describe LineNotifyRuby do
  it 'has a version number' do
    expect(LineNotifyRuby::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
